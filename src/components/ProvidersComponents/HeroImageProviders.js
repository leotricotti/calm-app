import styles from "./heroImageProviders.module.css";

function HeroImageProviders() {
  return (
    <div className={styles.imageContainer}>
      <div className={styles.heroImage}>K</div>
    </div>
  );
}

export default HeroImageProviders;
